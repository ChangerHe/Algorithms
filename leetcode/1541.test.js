const minInsertions = require("./1541.平衡括号字符串的最少插入次数");

describe("", () => {
  it("", () => {
    expect(minInsertions("(()))")).toBe(1);
    expect(minInsertions("(()))(()))()())))")).toBe(4);
  });
});
