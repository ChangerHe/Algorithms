const maxSlidingWindow = require("./239.滑动窗口最大值");

describe("", () => {
  it("", () => {
    expect(maxSlidingWindow([9, 10, 9, -7, -4, -8, 2, -6], 5)).toEqual([
      10, 10, 9, 2,
    ]);
  });
});
