const longestPalindrome = require("./5.最长回文子串");

describe("", () => {
  it("", () => {
    expect(longestPalindrome("babad")).toBe("aba");
    expect(longestPalindrome("cbbd")).toBe("bb");
  });
});
