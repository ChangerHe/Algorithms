const checkInclusion = require("./567.字符串的排列");

describe("", () => {
  it("", () => {
    expect(checkInclusion("ab", "eidbaooo")).toBe(true);
    expect(checkInclusion("ab", "eidboaoo")).toBe(false);
  });
});
